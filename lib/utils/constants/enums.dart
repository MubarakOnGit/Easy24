import 'package:flutter/material.dart';

/// LIST OF Enums

/// They cannot be created inside a class.

enum TextSizes { small, medium, large }

enum DeliveryOrderStatus { paymentConfirmed, onTheWay, delivered }

enum DineInOrderStatus { paymentConfirmed, readyForPickup, completed }

enum PaymentMethods { googlePay, applePay, visa, masterCard, creditCard }
